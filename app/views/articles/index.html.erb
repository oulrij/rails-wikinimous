<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <% @articles.each do |a| %>
      <div class="article">
        <h2><%= link_to a.title, article_path(a) %></h2>
        <p><%= a.content %></p>
      </div>
      <% end %>
    </div>
  </div>
</div>
